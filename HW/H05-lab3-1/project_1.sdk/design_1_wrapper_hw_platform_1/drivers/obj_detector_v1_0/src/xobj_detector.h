// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2015.2
// Copyright (C) 2015 Xilinx Inc. All rights reserved.
// 
// ==============================================================

#ifndef XOBJ_DETECTOR_H
#define XOBJ_DETECTOR_H

#ifdef __cplusplus
extern "C" {
#endif

/***************************** Include Files *********************************/
#ifndef __linux__
#include "xil_types.h"
#include "xil_assert.h"
#include "xstatus.h"
#include "xil_io.h"
#else
#include <stdint.h>
#include <assert.h>
#include <dirent.h>
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/mman.h>
#include <unistd.h>
#include <stddef.h>
#endif
#include "xobj_detector_hw.h"

/**************************** Type Definitions ******************************/
#ifdef __linux__
typedef uint8_t u8;
typedef uint16_t u16;
typedef uint32_t u32;
#else
typedef struct {
    u16 DeviceId;
    u32 Control_BaseAddress;
} XObj_detector_Config;
#endif

typedef struct {
    u32 Control_BaseAddress;
    u32 IsReady;
} XObj_detector;

/***************** Macros (Inline Functions) Definitions *********************/
#ifndef __linux__
#define XObj_detector_WriteReg(BaseAddress, RegOffset, Data) \
    Xil_Out32((BaseAddress) + (RegOffset), (u32)(Data))
#define XObj_detector_ReadReg(BaseAddress, RegOffset) \
    Xil_In32((BaseAddress) + (RegOffset))
#else
#define XObj_detector_WriteReg(BaseAddress, RegOffset, Data) \
    *(volatile u32*)((BaseAddress) + (RegOffset)) = (u32)(Data)
#define XObj_detector_ReadReg(BaseAddress, RegOffset) \
    *(volatile u32*)((BaseAddress) + (RegOffset))

#define Xil_AssertVoid(expr)    assert(expr)
#define Xil_AssertNonvoid(expr) assert(expr)

#define XST_SUCCESS             0
#define XST_DEVICE_NOT_FOUND    2
#define XST_OPEN_DEVICE_FAILED  3
#define XIL_COMPONENT_IS_READY  1
#endif

/************************** Function Prototypes *****************************/
#ifndef __linux__
int XObj_detector_Initialize(XObj_detector *InstancePtr, u16 DeviceId);
XObj_detector_Config* XObj_detector_LookupConfig(u16 DeviceId);
int XObj_detector_CfgInitialize(XObj_detector *InstancePtr, XObj_detector_Config *ConfigPtr);
#else
int XObj_detector_Initialize(XObj_detector *InstancePtr, const char* InstanceName);
int XObj_detector_Release(XObj_detector *InstancePtr);
#endif

void XObj_detector_Start(XObj_detector *InstancePtr);
u32 XObj_detector_IsDone(XObj_detector *InstancePtr);
u32 XObj_detector_IsIdle(XObj_detector *InstancePtr);
u32 XObj_detector_IsReady(XObj_detector *InstancePtr);
void XObj_detector_EnableAutoRestart(XObj_detector *InstancePtr);
void XObj_detector_DisableAutoRestart(XObj_detector *InstancePtr);


void XObj_detector_InterruptGlobalEnable(XObj_detector *InstancePtr);
void XObj_detector_InterruptGlobalDisable(XObj_detector *InstancePtr);
void XObj_detector_InterruptEnable(XObj_detector *InstancePtr, u32 Mask);
void XObj_detector_InterruptDisable(XObj_detector *InstancePtr, u32 Mask);
void XObj_detector_InterruptClear(XObj_detector *InstancePtr, u32 Mask);
u32 XObj_detector_InterruptGetEnabled(XObj_detector *InstancePtr);
u32 XObj_detector_InterruptGetStatus(XObj_detector *InstancePtr);

#ifdef __cplusplus
}
#endif

#endif
