// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2015.2
// Copyright (C) 2015 Xilinx Inc. All rights reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xobj_detector.h"

extern XObj_detector_Config XObj_detector_ConfigTable[];

XObj_detector_Config *XObj_detector_LookupConfig(u16 DeviceId) {
	XObj_detector_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XOBJ_DETECTOR_NUM_INSTANCES; Index++) {
		if (XObj_detector_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XObj_detector_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XObj_detector_Initialize(XObj_detector *InstancePtr, u16 DeviceId) {
	XObj_detector_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XObj_detector_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XObj_detector_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

